import React from 'react'

const SignIn = ({ onSubmit }) => {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm();

  return <div>Login</div>;
};

export default SignIn